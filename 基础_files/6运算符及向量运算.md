向量是R语言中最基础的数据结构,自然地我们会很关注向量的运算,事实上,R语言的强大优势就体现在向量运算上。这归功于R语言强大的向量化操作

6.1 算法运算符
```r
a <-c(1+2+3)
b <-c(4+5+6)
a + b

c <-c(1,2,3)
d <-c(4,5,6)
c + d

/ 相除
%% 两个向量求除
%/%两个向量相除求商

%*% 两个向量的内积,也称为点乘
a <-c(1,2,3)
b <-c(4,5,6)
a %*% b

a^b
```
循环补齐(recycling)
当两个向量长度不相等的时候,短的向量会循环补齐,抱持与长向量的长度一致后,再做运算.
```r
x <-c(1,2)
y <-c(10,20,30,40)
x + y
```
```r
```
```r
x <-c(1,2,3)
y <-c(10,20,30,40,50,60,70)
x + y
```
关系运算符
将第一向量的每个元素与第二向量的相应元素进行比较,比较的结果是布尔值,布尔值是"真"TRUE或"假"FALSE中的一个。
>检查第一向量的每个元素是否大于第二向量的相应元素
```r
a <-c(1,2,7)
b <-c(4,5,6)
a > b

```
逻辑运算符
```r
a <- c(TRUE,FALSE,FALSE)
b <-c(FALSE,TRUE,FALSE)
a | b
a & b
!a
#逻辑运算符&&和||只考虑向量的第一个元素,给出单个元素的向量作为输出。
#&&称为逻辑AND运算符.取两个向量的第一个元素,并且只有两个都为TRUE时才给出TRUE
```r
a <- c(TRUE,FALSE,FALSE)
b <-c(FALSE,TRUE,FALSE)
a && b
# ||称为逻辑OR运算符。取两个向量的第一个元素,如果其中一个为TRUE,则给出TRUE
a || b
a && b

a <- 1:10
a
typeof(a)
```
%in%此运算符用于判断元素是否属于向量
```r
c(2,3,7) %in% c(1,2,3,4,5)
```
is.na()判断是否为缺失值,R里缺失值用NA表示,NA就是not available

```r
is.na(c(1,2,NA,4,NA,6))
!is.na(x)
```
R语言里还有一些特殊值:Inf,NaN,NA和NULL
```r
1/0
0/0

```