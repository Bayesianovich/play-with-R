- list()将多个不同类型的对象创建为列表：
最常用的方法是用$,通过名字来访问元素

tidyverse系列中的purrr包为方便操作列表,提供了一些列操作类别函数
- pluck()同[[提取列表中的元素中的元素
- keep()保留满足条件的元素
- discard():删除满足条件的元素
- compact()删除空元素
- append()在列表末尾添加元素
- flatten()摊平列表(只摊平一层)
数据框(数据表)
- R语言中做统计分析的样本数据,都是按数据框类型操作的。
- 数据框是指若干行和列的数据集，它与矩阵类似，但并不要求所有列都是相同的类型;
- 本质上讲，数据框就是一个列表，它的每个成分都是一个向量，并且长度相同，以表格的形式展现。总之，数据框是由列向量组成，有着矩阵形式的列表。
- 数据框与最常见的数据表是一致的：每一列代表一个变量属性，每一行代表一条样本数据

R自带数据框是data.frame,建议改用更现代的数据框:tibble。tidyverse包都是基于tibble数据框。

- as_tibble()将data.frame,matix,各成分等长度的list,转换为tibble。

